pipeline{
	agent none
	stages{
		stage('Backend'){
			agent {
				docker {image 'maven:3.8.8-openjdk-17'}
			}
			steps{
				sh 'mvm --version'
			}
		}
		stage ('front-end') {
			agent { 
				docker {image 'node:16-alpine'}
			}
			steps {
				sh 'node --version'
			}
		}
	}
}